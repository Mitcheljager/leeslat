<%= render "admin/layout", heading: "Edit genre #{@genre.name}" do %>
  <%= render "form", genre: @genre %>

  <br>

  <div>
    <%= link_to "Show this genre", [:admin, @genre] %> |
    <%= link_to "Back to genres", admin_genres_path %>
  </div>
<% end %>
