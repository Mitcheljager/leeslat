<% content_for :title, "New listing" %>

<%= render "admin/layout", heading: "New listing" do %>
  <%= render "form", listing: @listing %>

  <br>

  <div>
    <%= link_to "Back to listings", admin_listings_path %>
  </div>
<% end %>
