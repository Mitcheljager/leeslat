<%= render "admin/layout", heading: "Edit book #{@book.title} by #{@book.authors_human_list}" do %>
  <%= render "form", book: @book %>

  <br>

  <div>
    <%= link_to "Show this book", @book %> |
    <%= link_to "Back to books", books_path %>
  </div>
<% end %>
