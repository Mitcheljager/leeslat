<%= render "admin/layout", heading: "Edit book \"#{@book.title}\" by #{human_list(@book.authors.pluck(:name))}" do %>
  <%= render "form", book: @book %>

  <br>

  <div>
    <%= link_to "Show this book", @book %> |
    <%= link_to "Back to books", books_path %>
  </div>
<% end %>
