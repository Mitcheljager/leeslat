<% content_for :title, "Genres" %>

<h1>Genres</h1>

<div id="genres">
  <% @genres.where(parent_genre: nil).each do |genre| %>
    <%= render genre %>
  <% end %>
</div>

<%= link_to "New genre", new_genre_path %>
